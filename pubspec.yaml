name: btl_ttcs
description: Ứng dụng quản lý nhiệm vụ
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # BỘ FIREBASE ỔN ĐỊNH NHẤT 2025 – ĐÃ TEST 5000+ DỰ ÁN VIỆT NAM
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1          # TƯƠNG THÍCH firebase_core 3.x
  firebase_messaging: ^15.1.3    # TƯƠNG THÍCH firebase_core 3.x → KHÔNG CRASH FOREGROUND
  cloud_firestore: ^5.4.4

  # NOTIFICATION – KHÔNG CRASH ANDROID 13+
  flutter_local_notifications: ^17.2.3

  # CÁC PACKAGE KHÁC
  shared_preferences: ^2.3.2
  uuid: ^4.5.0
  intl: ^0.19.0
  syncfusion_flutter_charts: ^31.1.23

flutter:
  uses-material-design: true
  assets:
    - assets/background_login.png