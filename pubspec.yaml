name: btl_ttcs
description: Ứng dụng quản lý nhiệm vụ
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.1.1
  cloud_firestore: ^6.0.2
  firebase_auth: ^6.1.0
  shared_preferences: ^2.2.2
  uuid: ^4.5.0
  firebase_auth_web: ^6.0.3  # Đảm bảo hỗ trợ web nếu cần
  firebase_auth_platform_interface: ^8.1.2  # Tùy chọn, để đảm bảo tương thích
  intl: ^0.19.0


dev_dependencies:
  flutter_test:
    sdk: flutter